# Antigravity Workspace Rule

Use `AGENTS.md` as the main repository playbook for this workspace.

- Read `AGENTS.md`.
- Read `docs/ai/skills/index.md` before substantial edits.
- Read `docs/ai/current-task.md` only when it contains active work.
- Load `project-context.md`, `architecture.md`, `decisions.md`, and subagent docs only when relevant.
- Prefer `./scripts/check.ps1` or `./scripts/check.sh` before finishing.
- If this file and `AGENTS.md` differ, treat `AGENTS.md` as the source of truth.
