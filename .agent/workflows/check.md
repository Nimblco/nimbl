---
description: Validates the active task brief and linked files
---
// turbo
1. Run the command: \`pnpm workflow check\`.
2. Review the terminal output.
3. If validation fails, automatically read the active task brief, attempt to fix the missing fields (e.g., status, relevant files), and run the check again.
4. Inform the user of the final validation outcome.
