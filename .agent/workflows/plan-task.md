# Plan Task

Use `AGENTS.md`, `docs/ai/subagents/planner.md`, and `docs/ai/tasks/TEMPLATE.md`.

Workflow:

1. summarize the task and constraints
2. inspect the relevant files and docs
3. identify the smallest safe implementation path
4. define file ownership and write scopes
5. capture acceptance criteria, validation steps, and risks in a task brief
