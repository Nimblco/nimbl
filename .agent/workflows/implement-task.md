# Implement Task

Use `AGENTS.md`, `docs/ai/subagents/implementer.md`, and the current task brief before coding.

Workflow:

1. confirm the assigned write scope and constraints
2. inspect the relevant files before editing
3. make the smallest safe implementation inside that scope
4. update docs if behavior, commands, or architecture changed
5. run the most relevant validation command
6. report what changed, what was validated, and any remaining risks
