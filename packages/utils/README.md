# Utils Package

Use this package for shared helpers, types, validation logic, and other small modules used by the product workspace.

Keep it framework-agnostic so both `apps/web` and `apps/api` can depend on it cleanly.

Create a local `package.json` here when the package becomes real.
