# UI Package

Use this package for shared UI primitives, design tokens, or components consumed by `apps/web` and any future product surface.

Keep it focused on reusable presentation code so it stays easy to swap or expand later.

Create a local `package.json` here when the package becomes real.
