---
applyTo: "scripts/**/*,**/*.ps1,**/*.sh"
---

When editing scripts:

- prefer commands that are readable for a teammate who did not author them
- keep setup and validation commands aligned with `docs/ai/commands.md`
- avoid introducing project-specific assumptions without documenting them
